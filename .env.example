# Local development (Vite proxies /api → localhost:5000 if unset)
VITE_API_URL=http://localhost:5000/api/v1

# Production (Vercel) — set this at BUILD time to your Render API URL:
# VITE_API_URL=https://finpilot-ai-backend.onrender.com/api/v1
